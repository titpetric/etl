package main

import (
	"context"
)

// outputRestore writes files to `config.Folder`, stdin should have .commit_id in JSON.
func outputRestore(ctx context.Context, command *Command) error {
	return nil
}
